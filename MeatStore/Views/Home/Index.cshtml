@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered meats of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var meat in Model.PreferredMeat)
    {
        @await Html.PartialAsync("MeatsSummary", meat)
    }
</div>